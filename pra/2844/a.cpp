#include <iostream>
#include <string.h>

using namespace std;

#define MAX 1000000007

int main(void) {

  int N, M, K;
  cin >> N >> M >> K;
  int mas[N];
  memset(mas, 0, N);
  int p[M];
  p[0] = 0;
  for (int i = 1; i <= M; i++) {
    cin >> p[i];
  }

  for (int i = 0; i <= M; i++) {
    rec(    

  }
  
  return 0;
}
