#include <iostream>
#include <vector>
using namespace std;

int main() {
	int n;
	cin >> n;
	vector<int> v(n, 0);

while :q


	/*
    // {0, 1, ..., n-1} の部分集合の全探索
    for (int bit = 0; bit < (1<<n); ++bit)
    {
		cout << bit << " " << bitset<4>(bit) << endl;
    }
	*/
	return 0;
}  
